@extends('admin.layout') @section('title','Compétences — Administration') @section('section','Compétences') @section('content')<div class="flex items-center justify-between"><div><h1 class="font-display text-3xl font-bold">Compétences</h1><p class="mt-2 text-sm text-slate-400">Gère les technologies affichées sur le portfolio.</p></div><a href="{{ route('admin.skills.create') }}" class="rounded-lg bg-amber-400 px-4 py-2.5 text-sm font-bold text-[#0b1120]">+ Nouvelle compétence</a></div><div class="mt-8 grid gap-4 sm:grid-cols-2 lg:grid-cols-3">@forelse($skills as $skill)<article class="rounded-xl border border-[#1f2c47] bg-[#131c31] p-5"><div class="flex items-start justify-between"><div><span class="text-xs uppercase tracking-wider text-blue-400">{{ $skill->category }}</span><h2 class="mt-2 font-display text-lg font-semibold">{{ $skill->name }}</h2></div><span class="text-xs text-slate-500">{{ $skill->is_visible ? 'Visible' : 'Masquée' }}</span></div><div class="mt-5 flex gap-3"><a class="text-sm text-blue-400" href="{{ route('admin.skills.edit',$skill) }}">Modifier</a><form method="POST" action="{{ route('admin.skills.destroy',$skill) }}">@csrf @method('DELETE')<button class="text-sm text-red-400" onclick="return confirm('Supprimer cette compétence ?')">Supprimer</button></form></div></article>@empty<div class="text-slate-500">Aucune compétence.</div>@endforelse</div><div class="mt-5">{{ $skills->links() }}</div>@endsection
