@extends('admin.layout') @section('title','Expériences — Administration') @section('section','Expériences') @section('content')<div class="flex items-center justify-between"><div><h1 class="font-display text-3xl font-bold">Expériences</h1><p class="mt-2 text-sm text-slate-400">Gère le parcours affiché sur le portfolio.</p></div><a href="{{ route('admin.experiences.create') }}" class="rounded-lg bg-amber-400 px-4 py-2.5 text-sm font-bold text-[#0b1120]">+ Nouvelle expérience</a></div><div class="mt-8 space-y-4">@forelse($experiences as $experience)<article class="rounded-xl border border-[#1f2c47] bg-[#131c31] p-5"><div class="flex flex-col justify-between gap-3 sm:flex-row"><div><span class="text-xs uppercase tracking-wider text-blue-400">{{ $experience->start_date->format('Y') }}{{ $experience->is_current ? ' — Maintenant' : '' }}</span><h2 class="mt-2 font-display text-xl font-semibold">{{ $experience->role }}</h2><p class="mt-1 text-sm text-slate-400">{{ $experience->company }}</p></div><div class="flex gap-3 text-sm"><a class="text-blue-400" href="{{ route('admin.experiences.edit',$experience) }}">Modifier</a><form method="POST" action="{{ route('admin.experiences.destroy',$experience) }}">@csrf @method('DELETE')<button class="text-red-400" onclick="return confirm('Supprimer cette expérience ?')">Supprimer</button></form></div></div><p class="mt-4 max-w-3xl text-sm leading-6 text-slate-400">{{ $experience->description }}</p></article>@empty<div class="text-slate-500">Aucune expérience.</div>@endforelse</div><div class="mt-5">{{ $experiences->links() }}</div>@endsection
