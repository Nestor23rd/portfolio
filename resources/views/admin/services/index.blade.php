@extends('admin.layout')
@section('title','Services — Administration') @section('section','Services')
@section('content')
<div class="flex items-center justify-between"><div><h1 class="font-display text-3xl font-bold">Services</h1><p class="mt-2 text-sm text-slate-400">Présente les prestations proposées sur l’accueil.</p></div><a href="{{ route('admin.services.create') }}" class="rounded-lg bg-amber-400 px-4 py-2.5 text-sm font-bold text-[#0b1120]">+ Nouveau service</a></div>
<div class="mt-8 space-y-4">@forelse($services as $service)<article class="flex flex-col justify-between gap-4 rounded-xl border border-[#1f2c47] bg-[#131c31] p-5 sm:flex-row sm:items-center"><div class="flex items-center gap-4"><span class="material-symbols-outlined text-2xl text-blue-400">{{ $service->icon }}</span><div><h2 class="font-display text-xl font-semibold">{{ $service->title }}</h2><p class="mt-1 max-w-2xl text-sm text-slate-400">{{ $service->description }}</p></div></div><div class="flex shrink-0 gap-3 text-sm"><span class="text-slate-500">{{ $service->is_visible ? 'Visible' : 'Masqué' }}</span><a class="text-blue-400" href="{{ route('admin.services.edit',$service) }}">Modifier</a><form method="POST" action="{{ route('admin.services.destroy',$service) }}">@csrf @method('DELETE')<button class="text-red-400" onclick="return confirm('Supprimer ce service ?')">Supprimer</button></form></div></article>@empty<div class="rounded-xl border border-dashed border-[#1f2c47] p-8 text-center text-slate-500">Aucun service.</div>@endforelse</div><div class="mt-5">{{ $services->links() }}</div>
@endsection
