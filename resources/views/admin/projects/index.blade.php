@extends('admin.layout') @section('title','Projets — Administration') @section('section','Projets') @section('content')<div class="flex items-center justify-between"><div><h1 class="font-display text-3xl font-bold">Projets</h1><p class="mt-2 text-sm text-slate-400">{{ $projects->total() }} projet(s) enregistré(s).</p></div><a href="{{ route('admin.projects.create') }}" class="rounded-lg bg-amber-400 px-4 py-2.5 text-sm font-bold text-[#0b1120]">+ Nouveau projet</a></div><div class="mt-8 overflow-x-auto rounded-xl border border-[#1f2c47] bg-[#131c31]"><table class="w-full min-w-[760px] text-left text-sm"><thead class="border-b border-[#1f2c47] text-xs uppercase tracking-wider text-slate-500"><tr><th class="p-4">Projet</th><th class="p-4">Catégorie</th><th class="p-4">Statut</th><th class="p-4">Actions</th></tr></thead><tbody class="divide-y divide-[#1f2c47]">@forelse($projects as $project)<tr><td class="p-4"><strong>{{ $project->title }}</strong><p class="mt-1 text-xs text-slate-500">{{ $project->slug }}</p></td><td class="p-4 text-slate-400">{{ $project->category }}</td><td class="p-4">{{ $project->is_published ? 'Publié' : 'Brouillon' }}{{ $project->is_featured ? ' · À la une' : '' }}</td><td class="p-4"><div class="flex gap-3"><a class="text-blue-400 hover:underline" href="{{ route('admin.projects.edit',$project) }}">Modifier</a><form method="POST" action="{{ route('admin.projects.destroy',$project) }}" onsubmit="return confirm('Supprimer ce projet ?')">@csrf @method('DELETE')<button class="text-red-400 hover:underline">Supprimer</button></form></div></td></tr>@empty<tr><td colspan="4" class="p-8 text-center text-slate-500">Aucun projet.</td></tr>@endforelse</tbody></table></div><div class="mt-5">{{ $projects->links() }}</div>@endsection
